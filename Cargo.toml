[package]
name = "server-r-agent-proto"
version = "0.1.0"
edition = "2021"
description = "Rust gRPC protocol definitions for server-agent communication"
license = "GPL-3.0"

[dependencies]
prost = "0.13"
tonic = "0.12"

[build-dependencies]
tonic-build = "0.12"
